# ==========================================
# CONFIGURACION GENERAL DEL SERVIDOR
# ==========================================
server.port=8080
spring.application.name=MediAid

spring.security.user.name=admin
spring.security.user.password=5567_Poli
spring.security.user.roles=ADMIN

spring.web.resources.static-locations=classpath:/static/,file:public/

# ==========================================
# CONFIGURACION DE BASE DE DATOS (PostgreSQL)
# ==========================================
spring.datasource.url=jdbc:postgresql://localhost:5432/mediaid
spring.datasource.username=usr_mediaid
spring.datasource.password=m3d1a1dESCOM
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Driver de PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# CONFIGURACION DE EMAIL (GMAIL)
# ==========================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=escomediaid@gmail.com
spring.mail.password=vcgs luie mjtx jzha
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ==========================================
# CONFIGURACION DE VARIABLES DE ENTORNO
# ==========================================
# Variable para definir la ruta donde se encuentra el servidor de react
# Ruta de Desarrollo
app.rutaReact=http://localhost:5173
# Ruta de Productivo
# app.ruta-react=https://135.224.184.142

# Variable para la espera de conexion y respuesta antes de error (ms) 60_000ms = 1 minuto
app.timeout=60000

# Variable para el tiempo de expiracion del token de sesion (ms) 3600_000ms = 1 hora
# No funciona ya que es un static, cambiar directamente en JwtUtil.java
app.expirationTime = 3600000

# Variable para el tiempo de expiracion del token de registro (minutos) 1440m = 1 dia
app.expRegistro = 1440

# Variable para el tiempo de expiracion del token de reset password (minutos) 1440m = 1 dia
app.expPasswordReset = 1440

# ==========================================
# CONFIGURACION DE JPA / HIBERNATE
# ==========================================
# Crea o actualiza automaticamente las tablas segun las entidades
spring.jpa.hibernate.ddl-auto=update
# Muestra las consultas SQL en consola (util durante desarrollo)
spring.jpa.show-sql=true

# ==========================================
# OPCIONAL: LOGGING (para depurar)
# ==========================================  
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG 


